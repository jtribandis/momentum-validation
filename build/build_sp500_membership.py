#!/usr/bin/env python3
import sys
raise SystemExit('NOT IMPLEMENTED: this target is wired but not complete. Implement this script against protocol_contracts/, schemas/, and config/, emit the expected artifact, then rerun the Make target.')
