# Data fidelity contract

All identity joins use permaticker. PIT membership, fields, classifications, data vintages, and terminal-event semantics must be recorded before selections or clone candidate sets are created. Unknown Sharadar field semantics are quarantined rather than assumed. Raw extracts are immutable and assigned a data-vintage identifier.
